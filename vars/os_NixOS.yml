---
# === Archlinux ===
os: NixOS

# === System configuration ===
username: wabri
group: users
home_dir: /home/wabri
ide_install: true
system_install: true
general_install: true

# === Configurations ===
ide_config:
  - alacritty
  - vim
  - vim-plug
  - neovim
  - neovim-plug
  - tmux
  - tpm
  - bash
  - zsh
  - git

system_config:

general_config:
  - neofetch
