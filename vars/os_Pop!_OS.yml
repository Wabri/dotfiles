---
# === Archlinux ===
os: Pop!_OS

# === System configuration ===
username: wabri
install_command: sudo apt-get install --yes
update_command: sudo apt-get update --yes
home_dir: /home/wabri
workspace_dir: Documents/Workspaces
default_shell: zsh
default_keyboard: us
ide_install: true
system_install: true
general_install: true

# === Packages ===
ide_packages:
  - git
  - alacritty
  - code
  - zsh
  - vim
  - tmux
  - fzf
  - ripgrep
  - exa
  - bat
  - nodejs

#ide_packages:

general_packages:
  - neofetch
  - wget
  - vagrant
  - virtualbox
  - htop
  - ncdu
  - duf
  - tldr
  - vlc

# === Configurations ===
ide_config:
  - git
  - alacritty
  - vim
  - vim-plug
  - tmux
  - tpm
  - bash
  - zsh
  - starship
  - ohmyzsh

system_config:
  - font_source_code

general_config:
  - neofetch
